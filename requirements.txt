# =============================================================================
# weilai-HealthCheck-Agent v2.0 依赖
# =============================================================================
# 本文件是依赖声明源；可重复安装必须使用 Python 3.12 和 requirements.lock。
# 更新本文件后运行：
#   uv pip compile requirements.txt --python-version 3.12 --generate-hashes \
#     --output-file requirements.lock
# =============================================================================

# --- Web 与数据合同 ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pyyaml>=6.0
python-dotenv>=1.0
httpx>=0.27

# --- MySQL 运行态与迁移 ---
sqlalchemy>=2.0,<2.1
greenlet==3.5.4
alembic>=1.13,<2.0

# --- MCP（v2.0 新增：统一事实来源）---
mcp>=1.0

# --- 契约校验（v2.0 新增：跨项目共享 JSON Schema）---
jsonschema>=4.22

# --- 可观测与稳定性（v2.0 新增）---
tenacity>=8.2
structlog>=24.1
orjson>=3.10
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25

pymysql>=1.1

# --- 测试 ---
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21

# 当前任务队列由 integrations/runtime_queue.py 使用 MySQL 实现。
