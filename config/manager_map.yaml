# 主管 → 下属映射
# key   = 主管 user_id（sys_user.id）
# value = 下属 user_id 列表
#
# 主管登录后，工作台里可以切换到"以下属身份"查看该下属的任务池。
# 空列表仍表示该用户是主管，只是当前尚未配置可查看的下属。
# 修改后重启服务生效。
#
# ⚠️ 本文件按部署环境本地维护，请勿把真实员工信息提交到公开仓库。
#
# 查 user_id: python3 -c "import sqlite3;c=sqlite3.connect('data/local/healthcheck.db');[print(r) for r in c.execute(\"SELECT id,user_name FROM sys_user\")]"

# 示例（请替换为本环境的真实映射）：
# <主管 user_id>:
#   - <下属 user_id>
#   - <下属 user_id>
