# S0 可信测试基线报告

> 日期：2026-07-31  
> 状态：测试基线通过；正式编码前仍需建立 Git 可恢复基线  
> 适用基线：`docs/07-巡检AgentV2最终改造实施方案.md`

## 环境

- Python：3.12.11；由 `.python-version` 和 `pyproject.toml` 固定；
- 依赖：`requirements.lock`，61 个解析包，包含 SHA-256 哈希；
- 锁文件 SHA-256：`b1f7d992ce6c3c9e1cc061414ad6e6a1814ebea6e28d56ae73cdacfcc6fd731e`；
- 测试：离线执行，不读取 `.env`，不连接生产 MCP、MySQL 或中控；
- 改造前恢复包：`../.recovery/weilai-HealthCheck-Agent-v2.0/20260731-s0-before/source.tar.gz`；
- 恢复包 SHA-256：`4e250effc793f60889a0ce1135d85ad988e29b17439def772169237065dc5d6f`。

## 验证结果

| 门禁 | 结果 |
|---|---|
| 合同导出漂移检查 | 通过，导出 Schema 与代码一致 |
| 合同测试 | 55/55 通过 |
| R2/R3/R7 与旧规则回归 | 20/20 通过 |
| 全量离线测试 | 219/219 通过；另有 4 项真实 MySQL 测试默认跳过 |
| MySQL 迁移静态测试 | 3/3 通过 |
| MySQL Repository 真实集成测试 | 2/2 通过（单独注入测试库 URL 执行） |
| S2 队列与生命周期真实集成测试 | 2/2 通过（单独注入测试库 URL 执行） |
| R7 关键窗口 | 1/3/7/14/30 天映射已锁定 |
| 锁文件重建 | 全新 Python 3.12.11 临时环境严格哈希安装成功 |

统一复验命令：

```bash
./scripts/verify_s0.sh
```

## 结论与边界

S0 测试质量门已恢复。当前目录尚不是 Git 仓库；开始 S1 业务代码修改前，必须经负责人
确认后初始化版本库并创建正式基线提交，或纳入现有远端仓库。当前恢复归档只能用于灾难
恢复，不能替代 Git 变更追踪。该结果也不证明自主 Scheduler、MySQL 运行态、正式 MCP
Adapter、Result Sink 或反馈 Inbox 已完成。中控 MCP v1.0 与最终巡检信号合同的冲突仍需
在 S5 前解决。
