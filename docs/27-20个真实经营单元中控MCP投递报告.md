# 20 个真实经营单元中控 MCP 投递报告

投递日期：2026-08-05  
工具：`submit_patrol_batch`  
合同：《巡检Agent_MCP接收巡检数据接口文档_v2.0.md》  
中控业务批次号：`PATROL-REAL-20260805-20ASIN-V2`

## 投递结果

- 使用法国补采单元完成 1 条真实探针：`1 SUCCESS / 0 FAILED`；
- 使用同一业务批次号提交剩余 19 条：`19 SUCCESS / 0 FAILED`；
- 合计：`20 SUCCESS / 0 PARTIAL_SUCCESS / 0 FAILED`；
- 20 个经营单元业务键全部唯一；
- 中控返回 20 个唯一 `listingId` 和 20 个唯一 `proposalId`；
- 所有回执业务码均为 `OK`，消息均为“接收成功”。

## 数据边界

- 经营单元身份、每日经营指标、事实快照、异常和数据缺口均来自 MySQL 最终巡检结果；
- 法国经营单元使用补采成功 Run，其余 19 个使用主重采批次；
- 共携带 8 条真实业务异常和 83 条真实数据缺口信号；
- 没有模拟负责人、经营模式、经营标签或专业执行建议；
- 当前没有价格 Agent，所有建议均为 `MANUAL_REVIEW_ONLY` 且
  `autoExecutionAllowed=false`；
- 首次建议主单与明细状态均为 `PENDING_APPROVAL`。

## 审计记录

脱敏审计文件：

- `artifacts/control-center-delivery/PATROL-REAL-20260805-20ASIN-V2-probe.json`
- `artifacts/control-center-delivery/PATROL-REAL-20260805-20ASIN-V2-remaining.json`

审计文件只保存载荷哈希、单元数量和中控结构化回执，不保存 MCP Token、数据库密码或
完整巡检业务载荷。

## 已知问题

中控运行时已经接受 v2.0 字段，但 `tools/list` 发布的输入 Schema 尚未声明
`proposal.anomalies` 和 `details[].anomalyUid`。这不影响本次实际接收，但会影响自动合同
发现与客户端生成，中控仍需同步修复公开 Schema。
