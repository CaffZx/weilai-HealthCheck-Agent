# P0 / P1 / P2 任务落地状态

> 对应改造方案 v1.0 §27「分阶段改造任务」。
> 本轮交付范围：**P0 + P1 全部完成**；P2 为下一轮，本文档标注了各项的当前处境。

图例：✅ 已完成　🟡 部分完成　⬜ 未开始（本轮不在范围内）

---

## P0：合同和主链路 —— ✅ 10/10 完成

| # | 任务 | 状态 | 落地位置 | 验证 |
|---|---|---|---|---|
| 1 | 新增 `core/contracts.py` | ✅ | `core/contracts.py`、`core/enums.py`、`core/errors.py` | `tests/contract/`（50 例） |
| 2 | 新增 MCP 客户端 | ✅ | `clients/mcp_client.py` | `tests/unit/test_facts.py` 封套解析 11 例 |
| 3 | 新增事实采集、归一化和快照 | ✅ | `facts/collector.py`、`normalizer.py`、`quality.py`、`snapshot.py` | 同上，含哈希稳定性与防篡改 |
| 4 | 新增经营模式 Agent 客户端 | ✅ | `clients/mode_agent_client.py` | 快照/单元一致性双校验 |
| 5 | 新增广告 Agent 客户端 | ✅ | `clients/advertising_agent_client.py` | 含 FROZEN 治理开关，见偏差 D-02 |
| 6 | 新增中控客户端 | ✅ | `clients/control_center_client.py` | 幂等头 + 状态白名单 |
| 7 | 新增 `PatrolOrchestrator` | ✅ | `inspector/orchestrator.py` | 13 步主流程 + 8 条降级规则 |
| 8 | 新增提交包接口合同测试 | ✅ | `tests/contract/test_schema_validation.py` | 用**上游 schema** 校验我们的实例 |
| 9 | 保留旧规则，通过适配器调用 | ✅ | `inspector/legacy_rule_adapter.py` | `tests/unit/test_legacy_regression.py` 14 例 |
| 10 | 跑通单经营单元全链路 | ✅ | `tests/integration/test_end_to_end.py::test_s1_full_success` | 离线端到端 |

---

## P1：建议和复盘 —— ✅ 8/8 完成

| # | 任务 | 状态 | 落地位置 | 验证 |
|---|---|---|---|---|
| 1 | 新增 `PatrolAnalyzer` | ✅ | `inspector/analyzer.py` | 30 例单测 |
| 2 | 新增跨域冲突检查 | ✅ | 同上，6 类检查 | 退出模式增长动作 / 库存不足放量 / 预算越界 等 |
| 3 | 新增 `ProposalBuilder` | ✅ | `inspector/proposal_builder.py` | 审批门升级、动作准入分流 |
| 4 | 新增中控复盘入口 | ✅ | `web/backend/routers/review_v1.py` | `tests/integration/test_api.py` |
| 5 | 新增 `ReviewOrchestrator` | ✅ | `inspector/review_orchestrator.py` | 31 例单测，四种 outcome 全覆盖 |
| 6 | 新增持久化任务队列 | ✅ | `integrations/task_queue.py` + `db.py` + `worker.py` | 幂等入队、乐观锁抢占、退避重试 |
| 7 | 新增失败重试和待投递队列 | ✅ | `integrations/pending_delivery.py` | 场景 6 集成测试：重投**原包** |
| 8 | 跑通执行后复盘 | ✅ | `tests/integration/test_end_to_end.py::test_s8_review_success_closes_cycle` | 含执行偏差检测 |

---

## P2：生产优化 —— ⬜ 本轮不在范围内

| # | 任务 | 状态 | 当前处境与下一步 |
|---|---|---|---|
| 1 | 旧 SQLite 状态迁移 | ⬜ | 迁不迁历史取决于待冻结事项 **F-12**。新库 schema 已就绪（`integrations/db.py`），旧 `event_pool` 与新 `signal_dedup` 目前并存互不干扰 |
| 2 | 旧前端接口退役 | 🟡 | 启动自动批量已停用；旧接口仍在服务，等中控页面上线后按第二/三阶段退役（见 `docs/01-改造对照表.md` §6） |
| 3 | 批量并发和限流 | 🟡 | MCP 客户端已有信号量限流（默认 6）与重试退避；`patrol.max_concurrent_units=20` 已配置。**缺**：跨经营单元的全局调度与背压 |
| 4 | OpenTelemetry 链路 | ⬜ | 依赖已加入 `requirements.txt`；代码里已有贯穿全链路的 `trace_id`，接入 SDK 只需在 `deps.py` 加 instrumentation |
| 5 | 规则效果统计 | ⬜ | 未开始。`signal_dedup` 表已记录 `recurrence_count` 和首末命中时间，是统计的基础数据 |
| 6 | 巡检误报和漏报监控 | ⬜ | 未开始。需要中控回流"人工标记误报"的信号，当前中控合同里还没有这个字段 |
| 7 | 历史案例回放 | ⬜ | 未开始。事实快照带 `content_hash`，具备可重放的前提 |
| 8 | LLM 解释能力 | 🟡 | 边界已在代码和配置里锁死（`llm.enabled=false`、`explanation_only=true`、允许润色的字段白名单）；**润色实现本身未接入**，当前一律用确定性模板 |

---

## 本轮额外完成（方案未列，但交接包要求）

| 项目 | 原因 | 落地位置 |
|---|---|---|
| 契约枚举镜像与一致性测试 | 交接包 `tests/test_contract_registry_consistency.py` 要求跨契约一致 | `core/enums.py` + `tests/contract/test_enum_mirror.py` |
| 四个确定性队列字段 + 排序 | ADR 0006「取消综合执行分采用确定性任务排序」 | `inspector/signal_builder.py` + `config/queue_policy.yaml` |
| 交接指令（HandoffDirective v2） | 交接包 08 §2.3 巡检交接路由表 | `inspector/issue_codes.py` |
| 身份派生算法与官方测试向量对齐 | `operating-unit-ref.v2.vectors.json` | `core/operating_unit.py` + 契约测试 |
| 共享 JSON Schema 导出 + CI 校验 | 方案 §26.2「三个项目共享同一份版本化 Schema」 | `scripts/export_contracts.py --check` |
| 字段血缘记录 | ERP MCP 字段名未冻结，需区分"没配上"与"确实没有值" | `facts/normalizer.py` 的 `FieldResolver` |

---

## 测试覆盖对照（方案 §26）

| 方案要求 | 状态 | 位置 |
|---|---|---|
| §26.1 单元测试 12 项 | ✅ 12/12 | `tests/unit/`（105 例） |
| §26.2 合同测试 5 项 | ✅ 5/5 | `tests/contract/`（50 例） |
| §26.3 集成测试 8 场景 | ✅ 8/8 | `tests/integration/test_end_to_end.py` |
| §26.4 扩展点位回归 | 🟡 Buy Box 待合同 | `tests/unit/test_legacy_regression.py` 已覆盖主图、图片、A+、链接、父子关系、属性、类目、合规、促销、价差、差评和卡位；Buy Box 仍缺“本店 Seller ID”对照，显式登记为 `INSPECTION_POINT_COVERAGE_LIMITED` |

当前全量离线测试见 `pytest -q`；无需调用外部 MCP 或业务数据库。
