# Amazon Agent 明显问题扫描报告

## 范围与限制

- 扫描对象：本项目本地代码、规则 YAML、提示词、知识库、SQLite Schema、同步逻辑、日志和前端接口。
- 扫描方式：使用 `amazon-agent-obvious-issue-scan` 进行只读审计，并对扫描器线索进行人工复核。
- 安全边界：未调用生产 MCP、ERP、Amazon API，也未修改生产数据。
- 测试现状：项目中未发现自动化测试文件，因此运行时结论缺少回归测试支撑。
- 扫描器原始结果：94 条线索，其中包含较多关键词误报；本报告仅保留经过人工复核的高价值问题。

## 执行摘要

- P0：未发现。
- P1：4 项，主要涉及跨店铺数据覆盖、全量同步数据丢失、数值精度和批量状态真实性。
- P2：3 项，主要涉及知识库索引漂移、同步幂等证据不足和测试缺口。
- 目前未发现会直接执行广泛广告修改的 P0 问题，但建议在接入广告写操作前优先解决 P1 项。

## P1 明显问题

### P1-001：事实表主键缺少店铺维度

- 分类：`confirmed-code-bug`
- 证据：`data/schema.sql:33`、`data/schema.sql:60`、`data/schema.sql:85`、`data/local_store.py:42`
- 观察：`daily_product_sales`、`daily_ad_product`、`sales_child` 的主键或冲突键不包含 `shop_account`，但写入字段包含店铺信息。
- 风险：同一 ASIN 在多个店铺存在时，后写入的数据可能覆盖先写入的数据；巡检可能使用错误店铺的销售、广告或目标数据。
- 最小验证：向同一数据库写入相同 ASIN/日期但不同店铺的两条记录，检查记录数及查询结果。
- 建议测试：增加跨店铺同 ASIN 隔离测试，并确认所有读取查询均携带店铺条件。

### P1-002：全量同步失败时清空本地快照

- 分类：`confirmed-code-bug`
- 证据：`data/sync_asin_owner.py:104`、`data/sync_asin_owner.py:121`、`data/sync_asin_owner.py:258`、`data/sync_erp.py:86`
- 观察：分页或子任务失败后，代码仍执行 `DELETE`，随后只写入已经成功返回的部分数据。
- 风险：一次部分失败可能删除完整的用户、负责人或 ERP 配置快照，造成产品消失、负责人过滤错误或判定基线缺失。
- 最小验证：模拟第 N 页请求失败，确认同步后本地表是否只剩部分数据。
- 建议测试：分页失败时保留旧快照；只有完整同步成功后才通过事务原子替换。

### P1-003：金额和 ACOS 使用二进制浮点及 SQLite `REAL`

- 分类：`probable-code-risk`
- 证据：`data/schema.sql:27`、`data/schema.sql:51`、`data/sync_erp.py:56`、`data/ad_state_reader.py:76`
- 观察：广告花费、销售额、ACOS、预算及目标值被转换为 `float`，并使用 SQLite `REAL` 存储或计算。
- 风险：阈值比较、金额累计和比例计算可能出现浮点边界偏差；代码没有明确金额、竞价和比例字段的 scale 及舍入模式。
- 当前可确认程度：已确认存在精度风险，但尚未发现由此产生错误结果的实际样本。
- 最小验证：使用 `0.1 + 0.2`、阈值相等值、略低值和略高值验证判定结果。
- 建议测试：定义统一数值精度契约，覆盖 ACOS、预算、花费和边界比较。

### P1-004：批量同步无法表达部分成功

- 分类：`confirmed-code-bug`
- 证据：`data/sync_azlisting.py:188`、`data/sync_azlisting.py:219`、`data/sync_azlisting.py:371`
- 观察：子体行数异常或单个子体请求失败时，函数仍可能记录 `ok` 并返回 `n, "OK"`，结果只保留总数和简化状态。
- 风险：后续巡检或运营无法区分完整成功、部分成功和失败，可能将不完整数据当作完整数据使用。
- 最小验证：模拟一个子体请求失败或返回缺失日期数据，检查返回状态和 `sync_log.status`。
- 建议测试：保留子任务级结果，实现 `SUCCESS`、`PARTIAL_SUCCESS`、`FAILED`，并区分提交成功与数据验证成功。

## P2 问题

### P2-001：知识库索引引用不存在的文件

- 分类：`design-risk`
- 证据：`knowledge/README.md:7`
- 观察：知识库 README 引用了多个实际不存在的文件，例如 `01_知识库总则与全局执行边界.md`；实际文件命名为 `01_模块0_关键术语.md` 等。
- 风险：按 README 路由检索时可能失败或加载错误模块，造成规范漂移。
- 最小验证：逐项解析 README 链接，并调用 `core/knowledge_loader.py` 读取对应文件。
- 建议测试：增加知识库索引完整性检查，阻止引用不存在的文件。

### P2-002：同步重试缺少业务幂等证据

- 分类：`probable-code-risk`
- 证据：`data/sync_azlisting.py:52`、`data/sync_asin_owner.py:61`、`inspector/scheduler/event_pool.py:98`
- 观察：MCP 会话失效时会自动重试；事件池有唯一 hash，但同步请求没有业务幂等键或执行结果确认机制。
- 风险：当前主要是读取路径，风险相对有限；未来接入写操作后，网络超时重试可能造成重复执行。
- 最小验证：模拟服务端已执行但客户端超时，确认重试是否会重复提交业务动作。
- 建议测试：为写操作设计稳定幂等键，并验证超时、重试和重复请求场景。

### P2-003：关键规则缺少自动化回归测试

- 分类：`test-gap`
- 证据：项目中未发现 `test_*.py`、`*_test.py` 或测试目录。
- 观察：跨店铺隔离、数据不足、ACOS 阈值、部分同步、事件复发和唯一键等关键行为没有自动化断言。
- 风险：后续修改可能悄然破坏数据隔离、状态真实性或业务边界。
- 建议测试：优先覆盖 P1-001、P1-002、P1-004，以及 ACOS 数据不足和阈值边界。

## 未确认但应补齐的证据

- 真实数据库中是否存在同一 ASIN 多店铺记录。
- MCP/ERP 返回的金额、ACOS、预算字段的实际单位和精度。
- 同步失败时业务是否允许继续使用旧快照。
- 当前是否存在任何会修改 Amazon 广告实体的执行接口。
- `event_pool` 是否已被实际巡检流程调用。
- 生产或测试环境是否已经暴露硬编码 API Key 等敏感配置问题。

## 推荐修复顺序

1. 修复全量同步的失败保护和原子替换。
2. 修复事实表跨店铺主键及相关查询条件。
3. 明确金额、ACOS、预算和竞价的精度与舍入契约。
4. 增加批量 `PARTIAL_SUCCESS` 状态和子任务结果保留。
5. 修正知识库索引并增加关键边界回归测试。

