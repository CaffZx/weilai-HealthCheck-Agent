# 业务巡检 Agent · 部署与使用说明

亚马逊商品业务巡检系统。自动检测 Listing 的 7 大类异常（链接状态、内容完整性、价格促销、库存可售、交易表现、合规属性、售后），输出结构化的运营任务卡。

---

## 一、部署（3 步）

### 方式 A：一键脚本（推荐）

```bash
unzip weilai-HealthCheck-Agent_*.zip
cd weilai-HealthCheck-Agent
chmod +x run.sh && ./run.sh
```

脚本自动完成：创建虚拟环境 → 安装依赖 → 初始化数据库 → 启动服务。

```bash
./run.sh              # 前台运行
./run.sh -d           # 后台运行
./run.sh --reload     # 开发模式（代码改动自动重启）
./run.sh -p 9000      # 指定端口
```

浏览器打开 **http://127.0.0.1:8000**。

### 方式 B：Docker

```bash
docker-compose up -d
```

数据库和日志挂载到本地 `data/local/` 和 `logs/`，重启不丢。

### 方式 C：手动

```bash
pip install -r requirements.txt
python -m uvicorn web.backend.main:app --host 0.0.0.0 --port 8000
```

> 💡 **包内已含生产数据**：`data/local/healthcheck.db`（584MB）包含 646 个 ASIN 的最新同步数据和批量巡检结果，解压即用，无需自己跑数据同步。

---

## 二、页面功能

### 主页面 `http://127.0.0.1:8000`

三栏布局：

| 栏 | 功能 |
|---|---|
| **左：知识库** | 6 个模块 MD，点击查看，修改后即时生效（热加载） |
| **中：ASIN 列表** | 646 个产品，支持搜索/筛选/排序 |
| **右：详情** | 产品信息 + 基础判定 + AI 介入检查 |

### 报告模式 `/report`

给运营看的只读页面，隐藏知识库、ASIN 列表等开发元素：

```
/report?userId=404              → 该负责人的巡检列表
/report?parentAsin=X&shopId=Y   → 单个产品的判定分析
/report?parentAsin=X&shopId=Y&userId=404  → 组合模式
```

---

## 三、判定流程

### 基础判定（代码管线）

点 ASIN 后自动触发，秒级出结果。流程：R2 异常检测 → R3 严重度判定 → R4 执行分计算 → 输出任务卡。

### AI 介入检查（LLM 管线）

对高优先级产品（P0/S0 异常）进行 DeepSeek 深度分析。知识库全文 + 产品数据 → 输出根因分析和处理建议。

---

## 四、定时任务（服务器上）

| 时间 | 任务 |
|------|------|
| 每天 02:00 | 全量数据同步 `sync_all` |
| 每天 04:00 | 批量代码巡检 |
| 每天 05:00 | 数据覆盖健康检查 |
| 每周一 06:00 | 负责人数据同步 |

---

## 五、目录结构

```
weilai-HealthCheck-Agent/
├── web/
│   ├── backend/main.py        ← FastAPI 后端（所有 API）
│   └── frontend/index.html    ← 单文件 SPA 前端
├── inspector/                 ← 巡检子系统
│   ├── inspector_main.py      ←   代码判定入口
│   ├── engine/                ←   判定引擎（R2-R6）
│   ├── scheduler/             ←   定时任务 + 事件池
│   └── rules/                 ←   YAML 热加载参数
├── core/
│   ├── llm_judge.py           ← LLM 判定引擎
│   └── knowledge_loader.py    ← 知识库热加载
├── data/                      ← 数据接入层
│   ├── local_store.py         ←   SQLite 读写
│   ├── mcp_client.py          ←   MCP 网关客户端
│   ├── sync_all.py            ←   全量同步编排
│   └── local/                 ←   运行时数据
│       ├── healthcheck.db     ←     SQLite（584MB，646 ASIN）
│       └── batch_cache.json   ←     批量巡检缓存
├── knowledge/                 ← 知识库 MD（热加载）
├── prompts/                   ← LLM 提示词模板
├── config/                    ← YAML 配置
├── scripts/
│   ├── run_daily.sh           ← 每日编排（02:00 crontab 触发）
│   └── cron_wrapper.sh        ← cron 任务统一日志包装
├── requirements.txt
└── .env                       ← 环境变量（含真实凭证）
```

---

## 六、扩展开发

### 改知识库

编辑 `knowledge/*.md`，保存即生效，不需要重启。刷新页面后下次判定自动使用新规则。

### 改判定参数

编辑 `inspector/rules/R*.yaml`，热加载。比如调整 ACOS S0 阈值：改 `R3_严重度阈值.yaml`。

### 改前端

编辑 `web/frontend/index.html`（单文件，CSS/HTML/JS 在一起），刷新浏览器即生效。

### 加新 API

在 `web/backend/main.py` 中添加 FastAPI 路由。

### 详细架构

见 `CLAUDE.md`。

---

## 七、常见问题

**Q：启动后 ASIN 列表为空？**
检查 `data/local/healthcheck.db` 是否存在。如果缺失，运行 `python -m data.sync_all` 从 MCP 拉数据。

**Q：判定结果很慢？**
代码判定（基础判定）是秒级的。LLM 判定（AI 介入检查）需要 15-30 秒，因为要处理 60K+ tokens 的知识库上下文。

**Q：想让别人远程访问？**
服务器上启动时已默认 `--host 0.0.0.0`。把服务器 IP 发给对方即可。⚠️ **不要暴露到公网**（.env 里有 API Key）。

**Q：如何更新数据？**
服务器上执行 `python -m data.sync_all`，或等凌晨 2 点自动同步。

---

## 八、注意事项

- `.env` 包含真实的 API Key 和数据库密码，**仅限内部使用，不得外传**
- `data/local/healthcheck.db` 包含生产数据，同样不要外传
- 部署更新用 `rsync -avz --exclude '.venv' --exclude 'data/local' <本地> root@server:/opt/weilai-HealthCheck-Agent/`
