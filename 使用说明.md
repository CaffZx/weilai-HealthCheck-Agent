# 业务巡检 Agent · 使用说明

一个可以在本地跑的亚马逊业务巡检 Agent 测试台。**离线运行**（数据是 70 个 ASIN 的固定样本），改知识库就能看判定结果差异。

## 一、准备（首次运行）

### 1. 装 Python 环境

需要 **Python 3.10 及以上**，Mac / Linux / Windows 都可以。

- Mac：`brew install python@3.11`（或用系统自带的 python3）
- Windows：https://www.python.org/downloads/ 下载安装

### 2. 装依赖

打开终端，进入解压后的项目目录：

```bash
cd weilai-HealthCheck-Agent
pip3 install -r requirements.txt
```

如果 pip 特别慢，用清华源加速：

```bash
pip3 install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> 💡 **已内置预同步数据库**：包内 `data/local/healthcheck.db` 是从 MCP 拉好的 70 个 ASIN 的真实数据（每日销售序列、库存、目标、Listing 基线等），判定时会自动采用，**开箱即用，无需自己跑同步**。想重新拉最新数据再运行 `python3 -m data.sync_daily --limit 70`。

### 3. 确认 API Key

`.env` 文件已经预置好了 DeepSeek 的 key，无需修改。想换成自己的 key，编辑 `.env`：

```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxx
```

## 二、启动

```bash
python3 -m uvicorn web.backend.main:app --port 8000
```

看到 `Uvicorn running on http://127.0.0.1:8000` 就是启动成功。

浏览器打开：**http://127.0.0.1:8000**

---

## 三、页面用法

### 三栏布局

| 栏 | 功能 |
|---|---|
| **左：知识库模块** | 点击任一模块查看内容（8 个 md 文件） |
| **中：采样 ASIN** | 70 个真实 ASIN 样本，顶部可过滤 ASIN/SKU/店铺/站点/阶段 |
| **右：详情** | 点 ASIN 后显示产品信息 + 判定结果 + 原始数据 |

### 单个 ASIN 判定

1. 中间栏点一个 ASIN
2. 右侧点 **"🔍 运行巡检判定"** 按钮
3. 等待 15–30 秒，DeepSeek 返回判定结果

结果包含：
- **优先级徽章**（P0 当天处理 / P1 3 天内 / P2 7 天内）
- **一句话结论 + 自然语言总结**
- **异常清单**（每项带 S0/S1/S2 严重度 + 白话原因 + 分数）
- **处理建议**（编号列表）
- **任务卡**（可直接派给运营）

**结果会自动缓存在浏览器里**，点其他 ASIN 后再点回来，还能看到上次的判定结果。刷新页面才会清空。

### 批量判定

1. 顶栏点 **"📊 批量判定"** 按钮
2. 弹层里选：
   - **数量**：5 / 10 / 20 / 70
   - **并发**：1 / 3 / 5（建议 3，太高可能被限流）
   - **范围**：当前过滤结果 / 所有 ASIN / 未跑过的
3. 点 **"▶ 开始批量判定"**，看进度条
4. 表格里每行显示：ASIN / 店铺 / 优先级 / 时限 / 分数 / 结论
5. 完成后点 **"⬇ 导出 CSV"** 下载全部已跑过的 ASIN 结果

**批量结果同样进缓存**，回到主页面点对应 ASIN 就能看详情。

---

## 四、改知识库测试新逻辑

**这就是这个项目最重要的功能**。

### 改哪里？

- `knowledge/*.md`（8 个模块）—— 判定规则的完整描述
- `config/rules.yaml` —— 热参数（ACOS 阈值、定位系数、优先级分数线等）

### 怎么改？

用任何文本编辑器打开对应文件，改完保存即可。**不需要重启服务**。

### 怎么验证改动生效？

1. 找一个之前已经跑过判定的 ASIN
2. 记住之前的结果（优先级 / 分数 / 建议）
3. 修改知识库或 rules.yaml
4. **点"🔄 重新运行判定"**
5. 对比新旧结果的差异

比如把 `03_异常严重度判定规则.md` 里的 ACOS S0 阈值从 30% 改成 50%，同一个 ASIN 可能就从 P0 降到 P1 甚至 P2。

---

## 五、目录结构

```
weilai-HealthCheck-Agent/
├── knowledge/           ← 知识库 8 个模块，改这里判定跟着变
├── config/
│   ├── rules.yaml       ← 热参数，改这里最直接
│   ├── mcp_tools.yaml   ← MCP 工具白名单（离线模式暂不用）
│   └── datasources.yaml ← 数据源配置（离线模式暂不用）
├── core/
│   ├── knowledge_loader.py   ← 读知识库 md
│   └── llm_judge.py          ← 调 DeepSeek 判定
├── data/
│   ├── fixture_loader.py     ← 读本地固定样本
│   └── mcp_client.py         ← MCP 客户端（离线模式暂不用）
├── web/
│   ├── backend/main.py       ← FastAPI 后端
│   └── frontend/index.html   ← 前端页面（单文件）
├── tests/
│   └── fixtures/demo/        ← 70 个 ASIN 的 MCP 采样数据（210 个 JSON）
├── requirements.txt
├── .env                 ← API Key（已预置）
└── 使用说明.md          ← 本文件
```

---

## 六、常见问题

**Q：为什么点了判定按钮，转圈很久？**
DeepSeek 每次调用要处理 60K+ tokens 的知识库上下文，正常 15–30 秒。如果超过 60 秒还没结果，可能是网络问题，控制台会打错误。

**Q：判定结果显示"parse_error"怎么办？**
LLM 偶尔会返回非标准 JSON，重新跑一次即可。多次出现说明 prompt 过长或知识库改动引入了矛盾条款。

**Q：批量判定跑到一半失败了怎么办？**
失败的行会标红，其他成功的仍在缓存里。可以再点一次"开始批量判定"，选"未跑过的"重跑失败项。

**Q：想让别人也能远程访问怎么办？**
把启动命令的 `--port 8000` 改成 `--host 0.0.0.0 --port 8000`，然后把你的局域网 IP 发给对方。**不要暴露到公网**（.env 里有 API Key）。

**Q：knowledge md 我改了但结果没变？**
1. 确认改的是 `knowledge/` 目录下的 md，不是 `使用说明.md`
2. 确认点了"🔄 重新运行判定"（不是从缓存回填）
3. 如果改的是 `.env` 或代码，需要重启 uvicorn（Ctrl+C 后重新运行）

---

## 七、反馈

发现 bug 或想加功能，直接跟维护者说。
