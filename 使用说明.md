# 业务巡检 Agent · 部署与使用说明

亚马逊商品业务巡检系统。自动检测 Listing 的 7 大类异常（链接状态、内容完整性、价格促销、库存可售、交易表现、合规属性、售后），输出结构化的运营任务卡 + 交互式工作台。

---

## 一、部署

```bash
# 安装依赖
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# 启动 FastAPI
python -m uvicorn web.backend.main:app --host 0.0.0.0 --port 8000
```

浏览器打开 **http://127.0.0.1:8000/?userId=<你的 user_id>**。

> 💡 **数据依赖**：`data/local/healthcheck.db` 是运行时数据。首次启动如果没有，运行 `python -m data.sync_all` 从 MCP 拉数据（约 1-2 小时）；或从服务器 rsync 现成的 db 回来。

---

## 二、六视图工作台

顶栏 6 tabs（普通运营 & 主管都能看，主管额外可切"查看下属"）：

| 视图 | 功能 |
|---|---|
| **今日任务** | 三栏：左侧任务池分优先级 + 中间任务表 + 右侧详情面板（含 🎯 广告决策 & 🔮 AI 深度分析 按钮） |
| **全部异常** | 全量事件池含已关闭，可筛选优先级/大类/状态，行点击弹抽屉看详情 |
| **调整复盘** | 到观察节点后由 Agent 对比执行前基线与后续巡检数据，输出效果和指标变化；运营仅确认保留动作、继续观察或重新处理 |
| **处理记录** | ACT/EVT 双 ID 可审计凭证；抽屉展示基础信息 + 执行前快照 + 处理轨迹时间线 |
| **数据看板** | 6 KPI（完成率/按时率/恢复率/P0按时/重复异常率）+ 每日柱图 + 异常大类分布 + 恢复率折线 + 动作有效率 + 高频异常产品 |
| **数据健康** | 每日表 + 快照表覆盖率，数据抓取完整性 |

### 报告嵌入模式 `/report`

```
/report?userId=404              → 该负责人的工作台
/report?parentAsin=X&shopId=Y   → 单产品详情
```

### 旧版技术台 `/legacy`

改 R2/R3/R4/R6 判定参数、直接看知识库、单产品跑判定用。

---

## 三、两条判定管线

### 基础判定（代码管线，秒级）
`R2 异常识别 → R3 严重度打档 → R4 执行分/优先级 → event_pool 落库 → R6 建议话术`

配置全部在 `inspector/rules/*.yaml`，改 yaml 热加载生效。

### AI 深度分析（LLM 管线，15-30 秒首次；后续秒返）
DeepSeek 拿代码判定结果作为权威事实 + 知识库全文 → 输出 P 级、执行分、异常明细（判断依据/处理建议/初步原因）。
- 结果落 `data/local/batch_cache.json`，磁盘持久化
- 前端事件详情看到 `📄 查看 AI 分析`（已存）或 `🔮 AI 深度分析`（未跑）

---

## 四、定时任务（服务器 crontab）

| 时间 | 任务 |
|------|------|
| 每天 02:00 | `scripts/run_daily.sh`：全量数据同步 → 批量代码巡检 → 数据覆盖健康检查（07:00 硬截止） |
| 每周一 06:00 | `sync_asin_owner`：负责人 & 用户列表 |
| 每周日 03:00 | 30/90 天日志清理 |

`config/settings.yaml` 里 `startup.auto_batch_inspect: false` 可禁用启动自动跑（推荐）；手动触发用 `POST /api/batch/inspect`。

---

## 五、目录结构

```
weilai-HealthCheck-Agent/
├── web/
│   ├── backend/
│   │   ├── main.py                 ← FastAPI app 装配 + startup
│   │   ├── common.py               ← 跨路由共享的常量/工具/DB 查询
│   │   ├── batch_cache.py          ← 批量巡检 + LLM 缓存
│   │   └── routers/                ← 按功能拆的 7 个路由
│   │       ├── pages.py            ←   /  /report  /legacy
│   │       ├── users.py            ←   /api/users  /api/reports (主管映射)
│   │       ├── knowledge.py        ←   /api/knowledge*
│   │       ├── fixtures.py         ←   /api/asins  /api/fixture  /api/sync-health
│   │       ├── judge.py            ←   /api/judge  /api/inspect  /api/batch/*
│   │       ├── tasks.py            ←   /api/tasks/*  /api/anomalies  /api/review/due
│   │       └── history.py          ←   /api/history*  /api/dashboard
│   └── frontend/
│       ├── index.html              ← 骨架 HTML（258 行）
│       ├── index.legacy.html       ← 旧技术台（改判定参数用）
│       └── assets/
│           ├── app.css             ← 全部样式
│           └── js/                 ← 按视图拆的 8 个 JS
│               ├── common.js       (state/api/utils/gate/switchView)
│               ├── boot.js         (启动)
│               └── view-today/all/review/history/dashboard/health.js
├── inspector/
│   ├── inspector_main.py           ← 代码判定入口（巡检单产品 / 巡检批量）
│   ├── engine/                     ← R2/R3/R4/R6 判定引擎
│   ├── scheduler/                  ← event_pool + daily_monitor
│   ├── rules/                      ← R2/R3/R4/R6 yaml 热加载参数
│   └── README.md                   ← 子系统架构说明
├── core/
│   ├── llm_judge.py                ← LLM 判定引擎
│   └── knowledge_loader.py         ← 知识库热加载
├── data/
│   ├── local_store.py              ← SQLite 读写 + schema.sql
│   ├── schema.sql                  ← 全部表定义（含 event_pool / task_action）
│   ├── fixture_loader.py           ← ERP 配置 → 产品列表
│   ├── sync_all.py                 ← 全量同步编排（并发 8 线程）
│   ├── sync_daily.py               ← 每日销售/广告快照
│   ├── sync_azlisting.py           ← 自然广告/月目标/库存预警/仓租/变体价促
│   ├── sync_asin_owner.py          ← 负责人/员工同步
│   ├── sync_erp.py                 ← ERP 配置同步
│   ├── ad_state_reader.py          ← 广告目标覆写值同步（读 MySQL）
│   ├── erp_config_reader.py        ← ERP 产品配置读取
│   ├── check_coverage.py           ← 数据覆盖率报告
│   └── local/                      ← 运行时数据（gitignore）
│       ├── healthcheck.db          ← SQLite (600MB+)
│       └── batch_cache.json        ← 批量巡检 + AI 结果缓存
├── knowledge/                       ← 知识库 MD（LLM 用，热加载）
├── prompts/                         ← LLM 提示词模板
├── config/
│   ├── settings.yaml               ← LLM 触发门槛 / 启动开关
│   └── manager_map.yaml            ← 主管 → 下属映射
├── scripts/
│   ├── run_daily.sh                ← 每日编排（02:00 crontab 触发）
│   └── cron_wrapper.sh             ← cron 任务统一日志包装
├── logs/                            ← 定时任务日志（30 天保留）
├── requirements.txt
└── .env                             ← 环境变量（含 API Key，禁外传）
```

---

## 六、扩展开发

### 改知识库
编辑 `knowledge/*.md`，保存即生效。下次 LLM 判定用新版。

### 改判定参数
编辑 `inspector/rules/R*.yaml`，热加载。比如调 ACOS S0 阈值：改 `R3_严重度阈值.yaml`。

### 改前端
- 骨架 HTML：`web/frontend/index.html`
- 样式：`web/frontend/assets/app.css`
- JS 按视图拆：`web/frontend/assets/js/view-*.js`
- 每次改前端要把 `index.html` 里 `?v=N` 版本号 +1（清浏览器缓存）

### 加新 API
- 在对应领域的 `web/backend/routers/*.py` 加路由；跨路由共享的辅助函数放 `web/backend/common.py`

### 加主管映射
- 编辑 `config/manager_map.yaml`：`{主管 user_id: [下属 user_id, ...]}`，重启 uvicorn 生效

---

## 七、常见问题

**Q：启动后 ASIN 列表为空？**
检查 `data/local/healthcheck.db` 是否存在。缺失就跑 `python -m data.sync_all`，或从服务器 rsync。

**Q：AI 分析很慢？**
首次调用 15-30s（DeepSeek 处理知识库+代码结果）。第二次点同产品秒返（走 `batch_cache.json`）。

**Q：巡检批量结果分数为 0？**
之前 `_upsert_event` 硬编码 `单异常执行分数=None`，跑一次 `python -m inspector.inspector_main`（全量）就自动回填分数。

**Q：改完前端刷新看不到？**
`/` 路由已经 `Cache-Control: no-cache`，F5 就够。JS/CSS 靠 URL 里 `?v=N` 版本号；改完 JS 记得 bump 版本号。

**Q：想让别人远程访问？**
服务器上启动时已默认 `--host 0.0.0.0`。⚠️ 别暴露到公网（.env 有 API Key）。

**Q：如何更新数据？**
服务器上执行 `python -m data.sync_all`，或等凌晨 2 点自动同步。

---

## 八、注意事项

- `.env` 有 API Key 和数据库密码，**不得外传**
- `data/local/healthcheck.db` 含生产数据，同样不外传
- 部署更新：`rsync -avz --exclude '.venv' --exclude 'data/local' --exclude 'logs' <本地> root@server:/opt/weilai-HealthCheck-Agent/`

---

## 九、当前功能进度

- ✅ Phase 1：今日任务/全部异常/复盘/数据健康 四视图 + 主管切换
- ✅ Phase 2：处理记录（ACT/EVT 双 ID）+ 数据看板（6 KPI）
- ✅ 广告决策跳转（`mcp-gateway.example.com`）
- ✅ AI 深度分析（DeepSeek + 磁盘缓存 + 复看）
- ✅ 复盘交互闭环（效果标记 + 二次调整回任务池）
- ⏳ 未做：R5 升降级规则、R_巡检频次、cause_analyzer 查因编排、trigger_registry 触发合并
